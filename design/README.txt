需要安装Java1.8环境和winPcap环境。
在命令行进到jar包所在目录，先使用命令:
java -jar name.jar   获取网卡信息。选择一个打开的网卡，地址形如192.168.**
然后使用命令：
java -jar Capture.jar,有三个参数，空格分开
第一个参数是网卡号，第二参数抓包数量（-1为无限抓包），第三个参数为过滤器
抓取特定的包如 tcp udp ip （小写）第三个参数也可以省略不写，抓取所有包。
